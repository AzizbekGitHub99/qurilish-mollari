import { Fragment } from "react";
import "./header.scss";

const Header = () => {
  return (
    <Fragment>
      <header className="header">Header</header>
    </Fragment>
  );
};

export default Header;
